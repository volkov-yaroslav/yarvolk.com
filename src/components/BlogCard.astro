---
import { formatDate } from '@utils/formatDate';

const { data, slug } = Astro.props;
const { title, date, image, category, Pinned } = data;
const coverImage = image || '/images/placeholder.webp';
const isPinned = Boolean(Pinned);
---

<article class="relative group text-center">
  {isPinned && (
    <div class="mb-3 flex items-center justify-center gap-1.5 text-[11px] uppercase tracking-wide text-black/55">
      <svg aria-hidden="true" width="12" height="12" viewBox="0 0 24 24" fill="none" class="shrink-0">
        <path d="M15.8 3.8a2 2 0 0 0-2.83 0L11 5.77l-1.97-1.97a2 2 0 1 0-2.83 2.83L8.17 8.6l-4.6 4.6a1 1 0 0 0 .7 1.7h5.02L11 20.73a1 1 0 0 0 1.9 0l1.71-5.83h5.02a1 1 0 0 0 .7-1.7l-4.6-4.6 1.97-1.97a2 2 0 0 0 0-2.83Z" fill="currentColor" />
      </svg>
      <span>pinned post</span>
    </div>
  )}
  <div class="relative overflow-hidden rounded-lg mb-8 aspect-[4/3]">
    <img
      src={coverImage}
      alt={title}
      width="620"
      height="500"
      class="duration-700 group-hover:scale-110 group-hover:-rotate-1 object-cover w-full h-full rounded-lg group-hover:brightness-75 bg-light/20"
      loading="lazy"
    />
  </div>
  <div class="px-6 transition-all duration-500 group-hover:opacity-60">
    <div class="flex flex-wrap items-center justify-center mb-4 space-x-5">
      <span class="inline-block text-sm rounded-full bg-[#efefef] px-3 py-1 capitalize">
        {category}
      </span>
      <span class="opacity-75 text-sm">{formatDate(date)}</span>
    </div>
    <h3 class="text-2xl leading-tight">
      <a href={`/blog/${slug}`} class="stretched-link">
        {title}
      </a>
    </h3>
  </div>
</article>
